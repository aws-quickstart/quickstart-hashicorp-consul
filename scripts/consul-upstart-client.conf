#! /bin/sh
### BEGIN INIT INFO
# Provides: consul-client
# Required-Start: $remote_fs $syslog
# Required-Stop: $remote_fs $syslog
# Default-Start: 2 3 4 5
# Default-Stop: 0 1 6
# Short-Description: ConsulClient 
# Description: This file starts and stops Consul client
# Maintainer tonynv@amazon.com
### END INIT INFO

export GOMAXPROCS=`nproc`

case "$1" in
 start)
   echo "Starting Consul Client"
 consul agent -config-dir /etc/consul.d -data-dir /opt/consul-data -join __MASTER_IP__ &
   ;;
 stop)
   echo "Killing Consul"
   pkill -9 consul 
   ;;
 restart)
   echo "Killing Consul"
   pkill -9 consul 
   pkill -9 consul
   sleep 2
   echo "Starting Consul Client"
 consul agent -config-dir /etc/consul.d -data-dir /opt/consul-data -join __MASTER_IP__ &
   ;;
 *)
   echo "Usage: consul {start|stop|restart}" >&2
   exit 3
   ;;
esac